# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.bak
.env
.env.local

# Node modules if we add tooling
node_modules/

# Python if we add scripts
__pycache__/
*.pyc
.venv/
venv/

# Test coverage reports
coverage/
.nyc_output/
*.lcov

# Docker - Now using containerization!
# Commit Docker configs, but not runtime secrets
k8s/secret.yaml
data/

# Cloned repositories
repos/

# Logs - keep directory structure but ignore large log files
# (We want to commit daily summaries but not huge debug logs)
*.log
agents/logs/system/

# Sensitive data
secrets/
*.key
*.pem
credentials.json
.env.test

# SECURITY NOTE: The following tokens are ALREADY EXPOSED in git history and need rotation:
#
# Proxmox API Tokens (EXPOSED - ROTATE THESE):
#   - Token Value: 8d1d247d-7798-4c02-b4a1-9ec74b90f5d9 (cortex-automation)
#     Found in: scripts/install-cortex-105.sh, scripts/install-cortex-104.sh,
#               scripts/remote-install-in-101.sh
#   - Token Value: b8cc165f-0153-43bb-a48a-5d7459587ca7 (root@pam!n8n)
#     Found in: scripts/deploy/bootstrap-k3s-cluster.sh
#   - Also in: docs/deployment/MISSION-EXECUTION-PLAN.md,
#              docs/deployment/k3s-deployment-file-tree.md
#
# n8n API Key:
#   - Not stored in repo (recommended: store in macOS Keychain)
#   - Retrieve with: security find-generic-password -a n8n -s cortex -w
#
# Action Required:
#   1. Rotate Proxmox tokens in Proxmox UI
#   2. Update scripts to use environment variables: $PROXMOX_TOKEN_VALUE
#   3. Store new tokens in .env (already gitignored)
#   4. See docs/security/SECRETS-ROTATION.md for rotation procedures

# Runtime coordination files (state, metrics, events)
coordination/events/*.jsonl
coordination/metrics/*.json
coordination/metrics/*.jsonl
coordination/metrics/learning/*.jsonl
coordination/history/
coordination/memory/working/*.json
coordination/orchestrator/state/*.json
coordination/pm-activity.jsonl
coordination/pm-state.json
coordination/token-budget.json
coordination/worker-pool.json
coordination/workforce-streams.json
coordination/dashboard-events.jsonl
coordination/health-alerts.json
coordination/patterns/failure-patterns.jsonl
coordination/patterns/pattern-index.json
coordination/governance/access-log.jsonl
coordination/knowledge-base/learned-patterns/patterns-*.json
coordination/knowledge-base/model-versions/
# Ignore most master knowledge-bases except achievement (for GitHub Actions)
coordination/masters/coordinator/knowledge-base/
coordination/masters/security/knowledge-base/
coordination/masters/development/knowledge-base/
coordination/masters/cleanup/knowledge-base/
coordination/masters/cicd/knowledge-base/
coordination/masters/inventory/knowledge-base/
coordination/masters/*/metrics/
coordination/masters/*/scans/
coordination/masters/*/output/
coordination/masters/shared-data/
coordination/tasks/pending/
coordination/worker-specs/zombie/
agents/logs/zombie-workers/

# Routing cascade runtime files
coordination/logs/
llm-mesh/training-data/*.jsonl
llm-mesh/training-data/*.py
llm-mesh/models/*.pt

# OpenTelemetry runtime files
coordination/observability/traces/*.jsonl
coordination/observability/metrics/*.jsonl

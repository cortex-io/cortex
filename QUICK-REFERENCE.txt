╔════════════════════════════════════════════════════════════════════╗
║           K3s HA Cluster Expansion - Quick Reference              ║
╚════════════════════════════════════════════════════════════════════╝

PROXMOX CONSOLE COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open: https://10.88.140.164:8006
Login: k3s / toor (for each VM console)

VM 303 (k3s-master02 -> 10.88.145.193):
sudo sed -i 's/10.88.145.190/10.88.145.193/g' /etc/netplan/00-installer-config.yaml
sudo hostnamectl set-hostname k3s-master02
sudo netplan apply && sudo reboot

VM 304 (k3s-worker03 -> 10.88.145.194):
sudo sed -i 's/10.88.145.191/10.88.145.194/g' /etc/netplan/00-installer-config.yaml
sudo hostnamectl set-hostname k3s-worker03
sudo netplan apply && sudo reboot

VM 305 (k3s-worker04 -> 10.88.145.195):
sudo sed -i 's/10.88.145.191/10.88.145.195/g' /etc/netplan/00-installer-config.yaml
sudo hostnamectl set-hostname k3s-worker04
sudo netplan apply && sudo reboot

VM 306 (k3s-master03 -> 10.88.145.196):
sudo sed -i 's/10.88.145.190/10.88.145.196/g' /etc/netplan/00-installer-config.yaml
sudo hostnamectl set-hostname k3s-master03
sudo netplan apply && sudo reboot

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AFTER IP FIXES COMPLETE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run from terminal:
cd ~/Projects/cortex
./complete-cluster-setup.sh

Or step-by-step:
./verify-ips.sh                  # Check IPs are fixed
./join-nodes-to-cluster.py       # Join nodes to cluster

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Check cluster:
ssh k3s@10.88.145.190 "kubectl get nodes -o wide"

Expected: 7 nodes (3 servers, 4 workers), all Ready

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Can't SSH after IP fix:
ssh k3s@<new-ip> "cat /etc/netplan/00-installer-config.yaml"
ssh k3s@<new-ip> "ip addr show"
ssh k3s@<new-ip> "sudo netplan apply"

Node won't join:
ssh k3s@<new-ip> "sudo journalctl -u k3s -f"           # servers
ssh k3s@<new-ip> "sudo journalctl -u k3s-agent -f"     # workers

Check master01:
ssh k3s@10.88.145.190 "kubectl get nodes"
ssh k3s@10.88.145.190 "sudo cat /var/lib/rancher/k3s/server/node-token"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

README-START-HERE.md         - Main guide with full instructions
K3S-SETUP-SUMMARY.md         - Technical details and architecture
PROXMOX-CONSOLE-STEPS.md     - Detailed console instructions
QUICK-REFERENCE.txt          - This file

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
